export class Database {
    constructor(param: (success) => void) {

    }

    isSupported() {

    }

    putBinaryFile(FILE_NAME: string, flashRAM: Uint8Array, param3: (success) => void) {
        
    }

    getBinaryFile(FILE_NAME: string, param2: (file) => void) {
        
    }
}
